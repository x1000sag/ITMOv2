Starting by locating the PR diff file and reading its contents to review the changes.
✱Glob "**/*.diff" (1 match)
→Read practices/practice_01/TRAINING_PR.diff [limit=2000]
↳ Loaded practices/practice_01/CONTEXT.md
Checking whether the referenced dependency 'review_service' exists and scanning for related code to understand context.
✱Glob "**/app/**/*.py" 
Ниже — замечания по PR, упорядочены по серьёзности, с привязкой к файлам/строкам из дифа.
Высокая серьезность
- app/api.py:35-38 — Отсутствует валидация входных данных. Обработка тела запроса как payload: dict и доступ по ключу payload["diff"] приведут к KeyError и 500 при отсутствии ключа или неверном типе. Рекомендация: ввести Pydantic-модель запроса с полем diff: str, FastAPI сам вернёт 422 при неверном вводе.
- app/review_service.py:19-22 — Нет обработки ошибок LLM. Любое исключение в llm.generate приведёт к 500 без понятного ответа клиенту. Рекомендация: отлавливать исключения и маппить на корректные коды (например, 502/503) с безопасным сообщением.
- app/api.py:35-38 — Нет аутентификации и лимитов. Публичный POST на LLM может быть использован для дешёвого DDoS/накрутки токенов. Рекомендация: защита эндпоинта (auth), rate limiting/quotas.
- Совместимость Python — использование dict[str, str] (app/api.py:36, app/review_service.py:19) требует Python 3.9+. Если проект на 3.8, это SyntaxError. Рекомендация: либо гарантировать 3.9+, либо заменить на Dict[str, str] из typing.
Средняя серьезность
- app/api.py:35-38 — Схема ответа и OpenAPI. Возвращается «сырой» dict[str, str] без response_model, документация будет неточной, валидации на выходе нет. Рекомендация: Pydantic-модель ответа (например, { comment: str }) и response_model=....
- app/api.py:30 — Инъекция зависимостей. Импорт глобального review_service из app.dependencies ухудшает тестопригодность и подмену зависимостей. Рекомендация: использовать Depends(get_review_service) с фабрикой/провайдером.
- app/review_service.py:19-22 — Непредсказуемый формат результата LLM. Промпт просит «find problems», но не фиксирует формат. Клиенты получат свободный текст, который сложно парсить/сравнивать. Рекомендация: формализовать требуемый JSON-формат в промпте и валидировать.
- app/api.py:35-38 — Ограничения на размер тела. Diff может быть очень большим; без ограничений возможны проблемы с памятью/временем обработки. Рекомендация: лимит размера запроса (на уровне ASGI/прокси) и/или предварительная валидация.
- app/review_service.py:19-22 — Риски prompt injection из содержимого diff. Стоит жёстче фиксировать системные инструкции, отделять контент диффа, использовать явные маркеры/delimiters и описывать игнорирование инструкций из диффа.
- Локализация — Промпт на английском, если продукт русскоязычный, это может снижать качество. Рекомендация: выбрать язык и закрепить.
Низкая серьезность
- app/review_service.py:10-13 — Изменение сигнатуры протокола с однострочного ... на многострочный — косметика; особой пользы нет, но не мешает.
- Логи/трейсинг отсутствуют. Рекомендация: хотя бы минимальный лог ошибок и корреляционные ID.
- Версионирование/путь API. POST /api/reviews без версионирования может создать проблемы в будущем. Рекомендация: /api/v1/reviews или routers c префиксом/тегами.
- Синхронность. Эндпоинт синхронный; если LLM-клиент блокирующий, ок, но при асинхронном клиенте лучше сделать async-варианты протокола/сервиса и эндпоинта.
Открытые вопросы
1. Какая целевая версия Python? (важно для dict[str, str] и typing.Protocol без typing_extensions)
2. Требуется ли аутентификация/авторизация на эндпоинте?
3. Какой формат результата ожидают клиенты? Достаточно ли одного поля comment, или нужен структурированный список проблем?
4. Какие ограничения по размеру diff и SLA на обработку?
5. Есть ли требования по языку ответов (RU/EN)?
6. Нужна ли потоковая выдача/прогресс для больших диффов?
Краткое резюме изменений, которые стоит внести
- Ввести Pydantic-модели для запроса и ответа, указать response_model в роуте.
- Добавить обработку ошибок LLM и корректные HTTP-коды.
- Перейти на DI через Depends(...) для ReviewService.
- Зафиксировать формат ответа LLM (JSON) в промпте и валидировать парсингом.
- Добавить аутентификацию/лимитирование и ограничение размера тела запроса.
- Уточнить минимальную версию Python или заменить аннотации на typing.Dict для совместимости.