> Некоторые пункты отклонены из-за того, что ИИ либо не знал требования проекта, либо не привёл упоминания на что ссылается

| reject | Причина |
| ------ | ------- |
| Совместимость Python — использование dict[str, str] (app/api.py:36, app/review_service.py:19) требует Python 3.9+. Если проект на 3.8, это SyntaxError. Рекомендация: либо гарантировать 3.9+, либо заменить на Dict[str, str] из typing. | не учитывает контекст проекта |
| - app/api.py:35-38 — Схема ответа и OpenAPI. Возвращается «сырой» dict[str, str] без response_model, документация будет неточной, валидации на выходе нет. Рекомендация: Pydantic-модель ответа (например, { comment: str }) и response_model=.... | Вмешивается в ответ функции. Верояятно в проекте ожидается именно такой "сырой" ответ модели |
| - app/api.py:35-38 — Ограничения на размер тела. Diff может быть очень большим; без ограничений возможны проблемы с памятью/временем обработки. Рекомендация: лимит размера запроса (на уровне ASGI/прокси) и/или предварительная валидация. | Не слишком риалистичная ситуация |
| - Локализация — Промпт на английском, если продукт русскоязычный, это может снижать качество. Рекомендация: выбрать язык и закрепить. Низкая серьезность | Никак не связано с проетком |
| - app/review_service.py:10-13 — Изменение сигнатуры протокола с однострочного ... на многострочный — косметика; особой пользы нет, но не мешает. | тогда зачем ты это упоминаешь, ии? |
| Версионирование/путь API. POST /api/reviews без версионирования может создать проблемы в будущем. Рекомендация: /api/v1/reviews или routers c префиксом/тегами. | Среагировал на API используемого сервиса, а не текущего продукта. не факт, что этот сервис контролируется командой разработчика |
| - Синхронность. Эндпоинт синхронный; если LLM-клиент блокирующий, ок, но при асинхронном клиенте лучше сделать async-варианты протокола/сервиса и эндпоинта. | Предположения без верефикации |