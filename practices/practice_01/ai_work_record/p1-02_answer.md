| Problem | risk | evidence | check |
|---|---|---|---|
| Отсутствует очистка секретов перед отправкой diff во внешний LLM (SEC-1) | Утечка токенов/ключей в сторонний сервис | app/review_service.py:19-22 — diff вставляется в prompt без маскирования; нет фильтра/редактора секретов | Передать diff, содержащий "token=abcd1234" и "-----BEGIN PRIVATE KEY-----"; ожидать, что в prompt они заменены на [REDACTED]; текущая версия отправляет исходные значения |
| Нет ограничения длины diff и ответа 413 (API-1) | Большие запросы перегружают LLM/сервер, вызывают таймауты и рост стоимости | app/api.py:35-38 — endpoint /api/reviews принимает payload["diff"] без проверки длины; нет возврата HTTP 413 | Отправить POST /api/reviews с diff длиной > 20000 символов; ожидать 413; текущая реализация возвращает 200/проксирует вызов в LLM |
| Формат ответа не соответствует контракту OUT-1 | Клиенты не смогут распарсить ответ; несовместимость API | app/review_service.py:20-22 — возвращается {"comment": answer} вместо полей summary, risks[], checks[] | Вызвать ReviewService.review("diff"); убедиться, что возвращаются поля summary, risks (≤3) и checks; текущая версия возвращает только "comment" |
